<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="navbar-brand">
    <% if signed_in? %>
      <%= link_to 'Dashboard', root_path %>
    <% end %>
  </div>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <% if signed_in? %>
          <%= link_to 'My Events', events_path %>
        <% end %>
      </li>
    </ul>
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <% if signed_in? %>
          <%= link_to 'Invited Events', invitations_path %>
        <% end %>
      </li>
    </ul>
    <ul class="nav navbar-nav navbar-right">
      <% if signed_in? %>
        <li><%= link_to 'Log out', sign_out_path, method: :delete %></li>
      <% else %>
        <li class="btn btn-outline-success my-2 my-sm-0 mr-1">
          <%= link_to 'Log in', sign_in_path %>
        </li>
        <li class="btn btn-outline-danger my-2 my-sm-0">
          <%= link_to 'Sign up', sign_up_path %>    
        </li>
      <% end %>
    </ul>
  </div>
</nav>